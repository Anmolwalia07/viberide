import Link from 'next/link';import {getPublicContent} from '@/lib/content';import {ContactEnquiryForm} from '@/components/ContactEnquiryForm';export default async function Contact(){const {site}=await getPublicContent();return <main className="pt-28"><div className="container section"><div className="grid gap-14 md:grid-cols-2"><div><div className="eyebrow">Contact</div><h1 className="serif mt-5 text-6xl md:text-8xl">Let&apos;s plan<br/><em>the journey.</em></h1><p className="mt-7 max-w-lg text-neutral-400 leading-7">Tell us where you need to go, when you need to travel, and what would make the journey easier.</p><div className="mt-10 grid gap-5"><div><div className="eyebrow">Phone</div><div className="mt-2 text-xl">{displayValue(site.phone)}</div></div><div><div className="eyebrow">Email</div><div className="mt-2 text-xl">{displayValue(site.email)}</div></div><div><div className="eyebrow">WhatsApp</div><div className="mt-2 text-xl">{displayValue(site.whatsapp)}</div></div></div><Link href="/booking" className="btn mt-10">Request a Chauffeur</Link></div><ContactEnquiryForm /></div></div></main>}
function displayValue(value:string){return value && !value.startsWith('[') ? value : 'Available on request';}
